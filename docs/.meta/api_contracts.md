# API Contracts

本项目不集成任何按量计费的外部模型 / 支付 / 地图 / 短信服务。

| Provider | 状态 | 说明 |
|---|---|---|
| （无） | N/A | Contract Gate 不适用。白噪音由浏览器 Web Audio 本地合成。 |

`ALLOW_TEST_COMPLETE` 是 QA 开关，不是外部 API mock。默认 `false`。
