# GoPomodoro · Mini Pomodoro Scrum

极客敏捷量化时间看板与里程碑燃尽图。开发端口：前端 `http://localhost:35172`。

```bash
docker compose up --build -d
```

测试账号：`geek@gopomodoro.dev` / `pomodoro123`

完整交付说明（启动、API、Mock 切换）将在 `/deploy` 阶段按七段式补齐。当前契约见 `docs/API.md`，需求见 `docs/Requirements.md`。
